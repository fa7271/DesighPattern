package this_is_java.lambda;

public class Calculator {
    public static int staticMethod(int x, int y) { // 정적 메소드ㅡ
        return x + y;
    }

    public int instanceMethod(int x, int y) { // 인스턴스 메소드
        return x + y;
    }
}
